# Copy this file to .env and fill in the correct connection string
# Example for local PostgreSQL
# Windows default local example (adjust user, password, host, port, db)
# DATABASE_URL format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://valler:Valler46!@localhost:5432/klasserom?schema=public"

# Optional: direct URL if you use connection pooling (e.g. with PgBouncer)
# DIRECT_URL="postgresql://valler:Valler46!@localhost:5432/klasserom?schema=public"

# Optional: shadow database used by Prisma Migrate (for safe migrations)
# SHADOW_DATABASE_URL="postgresql://valler:Valler46!@localhost:5432/klasserom_shadow?schema=public"

# ------------------------------------------------------------
# Example for Supabase (PostgreSQL managed)
# In Supabase project -> Settings -> Database -> Connection strings
#
# Use the POOLER connection (PgBouncer, transaction mode) for DATABASE_URL
# and the DIRECT (non-pooled) connection for DIRECT_URL.
# Prisma uses DIRECT_URL for migrations and long-lived operations.
#
# DATABASE_URL format (pooled):
# postgresql://USER:PASSWORD@HOST:PORT/DATABASE?pgbouncer=true&connection_limit=1
# (Optionally append &schema=public if you want to fix schema explicitly)
#
# DIRECT_URL format (non-pooled):
# postgresql://USER:PASSWORD@HOST:6543/DATABASE
# (Supabase direct port is typically 5432)
#
# DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@YOUR_PROJECT.supabase.co:5432/postgres?pgbouncer=true&connection_limit=1&schema=public"
# DIRECT_URL="postgresql://postgres:YOUR_PASSWORD@YOUR_PROJECT.supabase.co:5432/postgres?schema=public"
